version: '3'
services:

  mysql:
    image: "mysql:5.7.10"
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "admin"
      MYSQL_USER: "root"
      MYSQL_PASSWORD: "Root@123"
      MYSQL_DATABASE: "inventory"

